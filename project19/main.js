// 1. Ternary operator
const isAdult = age >= 18 ? 'Yes' : 'No';

// 2. Default greet parameter
function greet( name = 'Guest'){
    return `Hello ${name}`
}

// 3. Arrow Function
const add = (a,b) => a+b;

// 4. Destructuring Objects
const [first , second] = person

